# Dependencies
node_modules/

# Raw source photos — large originals (HEIC/JPG), kept on disk only so the
# photos can be re-optimized. The optimized photos/web/*.webp that the page
# actually loads ARE committed (webp is not ignored below).
*.heic
*.HEIC
*.jpg
*.JPG
*.jpeg
*.JPEG
*.png
*.PNG

# Windows internet shortcut (Google Photos album link)
*.url

# One-time working file produced while editing
countdown.stripped.html
